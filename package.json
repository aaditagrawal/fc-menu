{
  "name": "fc2-menus",
  "version": "0.1.0",
  "private": true,
  "//typescript": [
    "TypeScript 7 (native Go compiler). See CONTRIBUTING.md -> 'TypeScript 7' for the full story.",
    "The `@typescript/native-preview` entry is an ALIAS for typescript@7 GA -- Next only accepts a",
    "native compiler if that exact package name resolves, and the real native-preview package is a",
    "stale 7.0.0-dev build. TS 7 ships no programmatic JS API until 7.1, so Next CANNOT type-check",
    "the build itself; it detects the native compiler and skips type checking with a log line.",
    "THEREFORE: the `&& bun run typecheck` at the end of `build` IS the type-safety gate. Removing",
    "it means type errors ship silently. It must stay AFTER `next build` so .next/types route",
    "definitions exist and get checked.",
    "WHEN TS 7.1 SHIPS AND NEXT SUPPORTS IT: replace the alias with a plain \"typescript\": \"^7.x\"",
    "devDependency, point `typecheck` at node_modules/typescript/bin/tsc, and drop the",
    "`&& bun run typecheck` from `build` since Next will type-check natively again."
  ],
  "scripts": {
    "dev": "next dev --turbopack",
    "dev:portless": "portless run",
    "build:menu-data": "node scripts/build-menu-data.mjs",
    "build": "node scripts/build-menu-data.mjs && next build && bun run typecheck",
    "start": "next start",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt --write \"**/*.{js,mjs,cjs,ts,tsx,jsx}\" --ignore-path .gitignore --no-error-on-unmatched-pattern",
    "format:check": "oxfmt --check \"**/*.{js,mjs,cjs,ts,tsx,jsx}\" --ignore-path .gitignore --no-error-on-unmatched-pattern",
    "typecheck": "node_modules/@typescript/native-preview/bin/tsc --noEmit"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.3.3",
    "@tanstack/query-sync-storage-persister": "^5.101.4",
    "@tanstack/react-query": "^5.101.4",
    "@tanstack/react-query-persist-client": "^5.101.4",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.30.0",
    "next": "^16.3.0",
    "next-themes": "^0.4.6",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@tailwindcss/typography": "^0.5.20",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@typescript/native-preview": "npm:typescript@^7.0.2",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "typescript": "7.0.2"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide",
    "unrs-resolver"
  ]
}
